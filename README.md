# ВНИМАНИЕ!
Сейчас ведется разработка второй версии. Она прикреплена как сабмодуль LOCATION_v2.

## Зачем это сделано?

* 1. Дабы не потерять уже проделаную работу которая никак не пересекается со второй версией и планирующимися там функциями.

* 2. Этот сервер будет модифицирован для преренаправления трафика в компьютерное приложения из LOCATION_v2.

# 📍 Location Data Logger Server 

Сервер для сбора и визуализации данных геолокации с мобильных устройств. Система принимает данные от мобильного приложения через REST API и предоставляет удобный веб-интерфейс для анализа собранных данных.

---

## ✨ Возможности

* **📡 Прием данных** — REST API endpoint для приема JSON данных с координатами.
* **📊 Веб-интерфейс** — Табличное отображение всех полученных данных.
* **🗺️ Интерактивная карта** — Визуализация всех точек на карте OpenStreetMap.
* **📱 Поддержка источников** — GPS, сеть, WiFi и другие провайдеры.
* **🎨 Цветовая маркировка** — Разные цвета маркеров в зависимости от источника данных.

---

## 🚀 Быстрый старт

### Предварительные требования
* Python 3.8 или выше
* pip (менеджер пакетов Python)

### Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone --recursive <url-репозитория>
    cd <имя-папки>
    ```

2.  **Установите зависимости:**
    ```bash
    pip install fastapi uvicorn pydantic
    ```

3.  **Запустите сервер:**
    ```bash
    python main.py
    ```
    > Сервер запустится по адресу: `http://localhost:8000`

---

## 📚 API Endpoints

### `POST /push`
Принимает данные геолокации от мобильного приложения.

**Тело запроса (JSON):**
```json
{
    "accuracy": 12.5,
    "latitude": 55.7558,
    "longitude": 37.6173,
    "provider": "gps",
    "recordedTime": 1709123456789,
    "source": "mobile_app",
    "timestamp": 1709123456789
}
```

### Другие запросы:
| Метод | Путь | Описание |
| :--- | :--- | :--- |
| **GET** | `/log` | HTML-таблица со всеми записями |
| **GET** | `/data` | Все данные в формате JSON |
| **GET** | `/map` | Интерактивная карта со всеми точками |

---

## 📁 Структура данных
Каждая запись содержит:
* `latitude` / `longitude` — широта и долгота.
* `accuracy` — точность в метрах.
* `provider` — провайдер (gps, network, passive и т.д.).
* `recordedTime` — время записи на устройстве.
* `source` — источник данных.
* `timestamp` — временная метка сервера.

---

## 🛠 Технологии
* **Backend:** [FastAPI](https://fastapi.tiangolo.com/) (Python)
* **Карты:** [Leaflet](https://leafletjs.com/) + OpenStreetMap
* **Хранение:** JSON-файл (`data_log.json`)
* **Иконки:** Leaflet.awesome-markers

---

## 📦 Интеграция с мобильным приложением
Репозиторий содержит мобильное приложение как субмодуль. Для работы с ним:

```bash
# Инициализация субмодуля
git submodule init
git submodule update
```


